# QA検査結果(2026-07-08)

## 検査対象

- タスク: T1〜T7(全ゲーム・共通UI・README)
- 検査方法: ローカルHTTPサーバーで配信し、375px(スマホ)ビューポートの実ブラウザで全画面を操作して検証

## 結果(1回目)

| 項目 | 判定 | メモ |
|------|------|------|
| 動作(正常系) | ✅ | 参加者入力(追加/削除/重複連番/2〜12人制限)、4ゲームすべての進行、結果画面、「もう一回」を実機確認。ルーレットは盤面の停止位置と結果表示の一致を検証、あみだくじは経路計算と表示支払者の一致を検証、チンチロは「同格で振り直し」フローまで確認 |
| 動作(異常系) | ❌ | 下記2件の差し戻し事項 |
| セキュリティ | ✅ | 外部通信なし・秘密情報なし・入力値バリデーションあり。textContent使用でHTML注入なし |
| SPEC準拠 | ✅ | 命名規則・フォルダ構造・計画ファイル構成どおり。計画外の機能追加なし |
| 使いやすさ | ✅ | エラーメッセージ明確、READMEの手順で起動可能。※軽微: ルーレット左半分の名前ラベルが逆さ表示になる(視認性は保たれるため対応任意) |

## 差し戻し事項(1回目)

1. **[重大] ハイ&ロー: 最終カードめくり直後の連打で幽霊カードが混入する**
   - 再現: 最後のプレイヤーがカードをめくった後、結果画面に遷移するまでの約0.9秒間にカードを再タップ
   - 実測: `results` に `{name: "undefined", card: "10♦"}` が追加された。幽霊カードが最弱だった場合「undefinedさんのおごり!」と表示される
   - 原因: 最終めくり後に `isAnimating` が false に戻り、`.is-disabled` はCSSの `cursor` 変更のみでクリックを止めていない
2. **[重大] 全ゲーム共通: アニメーション中に「ゲーム選択に戻る」で戻っても、後から結果画面に強制遷移する**
   - 再現: ルーレット回転中に「← ゲーム選択に戻る」→ ゲーム選択画面に戻れるが、約6秒後に勝手に結果画面へ飛ばされる
   - 原因: 各ゲームのタイマー/アニメーションコールバックが画面離脱後も生きていて `showResult()` を呼ぶ。チンチロ・ハイ&ロー・あみだくじの遅延処理も同構造

## 再検査結果(修正後)

DEVによる修正(ハイ&ローの再タップガード+`pointer-events: none`、全ゲームへの世代トークン方式の導入)を、実ブラウザ(375px)で再検証した。

| 項目 | 判定 | メモ |
|------|------|------|
| 差し戻し1(幽霊カード) | ✅ 修正確認 | 最終めくり直後の危険な窓で5連打しても結果は3件のまま。`undefined` の混入なし、支払者表示も正常 |
| 差し戻し2(強制遷移) | ✅ 修正確認 | ルーレット回転中に離脱後、10秒間監視しても結果画面へ飛ばされない。同ゲームへの再入場も正常 |
| 回帰(正常系) | ✅ | 4ゲームすべて完走して支払者が決定。チンチロの振り直し、ハイ&ローの引き直し、「もう一回」も正常。コンソールにエラー・警告なし |

## 総合判定

**合格(完成)** — SPEC.md の完成基準(全タスク実装済み・実機での動作確認・QA通過・README整備)をすべて満たした。

### 備考(対応不要の軽微事項)

- ルーレット左半分の名前ラベルが逆さ向きに描画される(視認性は保たれるため今回は対応せず)
- あみだくじの到達分布はスタート位置に近い列へやや偏る性質があるが、支払い枠自体が一様ランダムなため公平性は担保されている

---

# QA検査結果 第2弾(2026-07-08 追記・効果音と演出強化 T8〜T12)

## 検査対象

- タスク: T8〜T12(効果音基盤・ルーレット音・ハイ&ロー絞り演出/音・チンチロ3回振り/音・あみだくじ爆弾表示/横線後出し/音)
- 検査方法: 375pxの実ブラウザで、SoundFx各メソッドの発火回数を計測しつつ全ゲームを操作。演出はCSS計算値・canvas画素・clip-pathの実測で検証

## 結果

| 項目 | 判定 | メモ |
|------|------|------|
| T8 効果音基盤 | ✅ | `SoundFx` が5メソッドを公開。初回タップで AudioContext を生成/resume。未対応環境でも無音で進行(例外なし)。script読み込み順も正しい |
| T9 ルーレット「がらがら」 | ✅ | 回転開始で unlock 1回。tickが減速に同期して発火(0.7秒時点8回→全16回、序盤密・終盤疎)。結果と盤面も一致 |
| T10 ハイ&ロー 絞り演出+シャカ音 | ✅ | clip-path が inset(100%→0) を1.2秒で下から開く(途中44%隠れ→0px)。cardSqueeze が各めくりで1回。4人完走・最弱者が支払い |
| T11 チンチロ 3回振り+着地音 | ✅ | 目無しは「あと○回振れる」で同一プレイヤー継続、3回目で確定。役/目が出れば即確定。diceLandが各振りで発火。最弱判定も正常 |
| T12 あみだくじ 爆弾/後出し/音 | ✅ | 下端に💣1つ+✅を初期表示。横線はreveal前0px→reveal後2235px出現。amidaTraceが各人1回。支払者=事前計算と一致 |
| 回帰(既存機能) | ✅ | 4ゲーム完走・引き直し/振り直し・もう一回・世代トークン(離脱で結果画面へ飛ばない)いずれも維持。コンソールエラーなし |

## 総合判定

**合格(完成)** — 改善第2弾のT8〜T12をすべて実機で確認し、既存機能の回帰もなし。

### 備考

- 効果音は初回タップ(unlock)後に鳴る仕様(ブラウザの自動再生制限を順守)。音自体はコードでは鳴らせないため「発火回数と経路」で検証したが、音色バランスは実機の耳での確認が理想
- チンチロのルールは「1人最大3回・目無しのみ振り直し・役/目/ヒフミは確定」に更新済み

---

# QA検査結果 第3弾(2026-07-08 追記・演出のリアル化 T13〜T15)

## 検査対象

- タスク: T13(ハイ&ロー 本物のトランプめくり)・T14(チンチロ お椀にサイコロ落下)・T15(あみだくじ 中間隠し)
- 検査方法: 375pxの実ブラウザで、CSS計算値・transform行列・canvas画素・covers表示状態・効果音発火回数を実測

## 結果

| 項目 | 判定 | メモ |
|------|------|------|
| T13 3Dトランプめくり | ✅ | perspective:1000px・preserve-3d・transition 0.8s transform。clip-path撤去。めくると transform が matrix3d(rotateY進行)→ rotateY(180°) で裏から表へ回転、表面(例10♥)表示。効果音は `cardFlip` に差し替わり各めくりで発火。4人完走・最弱者が支払い |
| T14 お椀+サイコロ落下 | ✅ | お椀(260×150)を描画しサイコロ3つが内側に収まる。振ると `is-dropping`+`dieDrop` 落下アニメが走り、着地で `diceLand()` 発火。3回振りルール(目無し→あと○回)・最弱判定も維持。375pxで崩れなし |
| T15 あみだ中間隠し | ✅ | 選択中は上端・下端の線スタブのみ表示、中央60%を縞模様パネル+「？？？」で隠蔽(画素検査: 上10%線あり/中50%線なし/下90%線あり)。💣/✅と「選ぶ」は表示。結果を見るで覆いが消え全縦線+横線が出現→トレース→支払者一致。「もう一回」で覆い・爆弾位置が再抽選され選択フェーズへ復帰 |
| 回帰(既存機能) | ✅ | ルーレット完走(tick音22回)・チンチロ振り最中に離脱で結果画面へ飛ばない・4ゲーム完走・引き直し/振り直し・もう一回・世代トークン維持。コンソールエラー0件 |

## 総合判定

**合格(完成)** — 改善第3弾のT13〜T15をすべて実機で確認。既存機能の回帰もなし。公開URLへ反映済み。
