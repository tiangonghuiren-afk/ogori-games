# タスク一覧 — ogori-games

## 今日やること

- [x] T1: 画面骨格と共通UI — 完了条件: `src/index.html` を開くと「参加者入力画面」が表示され、名前の追加(2〜12人)・削除ができ、2人以上で「ゲーム選択へ」ボタンが有効になる。ゲーム選択画面に4ゲームのボタンが並ぶ
- [x] T2: 共通の結果画面と画面遷移 — 完了条件: どのゲームからも共通の結果画面(支払者名を大きく表示)に遷移でき、「もう一回」(同じゲームを再実行)と「ゲーム選択に戻る」が動作する
- [x] T3: 男気ルーレット — 完了条件: 参加者名が扇形に並んだルーレットが回転アニメーション後に停止し、当たった1人が結果画面に表示される(T1・T2完了後)
- [x] T4: ハイ&ロー(カード引き) — 完了条件: 「○○さんの番」表示 → タップでカードをめくる、を全員分繰り返し、一番弱いカードの人が支払者になる。同点最下位は該当者のみで引き直しになる(T1・T2完了後)
- [x] T5: チンチロ — 完了条件: 各自タップで3つのサイコロを振り、役(ピンゾロ>ゾロ目>シゴロ>通常の目>目無し>ヒフミ)で判定され、一番弱い人が支払者になる。最下位が複数なら該当者のみで振り直しになる(T1・T2完了後)
- [x] T6: あみだくじ — 完了条件: 参加者ごとに上の枠を選び、全員選んだら線をたどるアニメーションが流れ、「支払い」マスに着いた人が支払者になる。横線はランダム生成される(T1・T2完了後)
- [x] T7: README作成と仕上げ — 完了条件: `README.md` に起動方法と各ゲームのルールが書かれ、スマホ幅(375px)で全画面が崩れず操作できる(T3〜T6完了後)

## 改善リクエスト第2弾(2026-07-08 追加・効果音と演出強化)

- [x] T8: 効果音基盤(`src/js/sound.js` 新規) — 完了条件: WebAudio API で音を合成する共通モジュールを作る。外部ファイル・外部通信なし。AudioContext は初回のユーザータップ時に生成/resume する(ブラウザの自動再生制限対策)。`SoundFx.rouletteTick()` / `SoundFx.cardSqueeze()` / `SoundFx.diceLand()` / `SoundFx.amidaTrace()` / `SoundFx.unlock()` を公開する。`index.html` の script 読み込みに `sound.js` を app.js の直後(各ゲームjsより前)に追加
- [x] T9: ルーレット「がらがら」音 — 完了条件: 回転中、セグメント境界がポインターを通過するたびに短いクリック音が鳴る。減速に同期してクリック間隔が自然に伸びる(「がらがらがら…がら…がら」)。回転開始タップで AudioContext を unlock する
- [x] T10: ハイ&ロー「シャカ」音+下から絞る演出 — 完了条件: カードをめくる瞬間に「シャカ」というスライド音が鳴る。めくり演出を現在のY軸回転から「下から上へゆっくり絞って開く」演出に変更する(約1.2秒、下端から表面が徐々に見えてくる)。既存の結果判定・引き直し・連打防止は壊さない
- [x] T11: チンチロ お椀落下音+3回振りルール — 完了条件: (a)サイコロ着地時に「チリンチリン」という金属的な着地音(3個が少しずつずれて鳴る)。(b)ルール変更: 1人あたり最大3回まで振れる。役または通常の目が出たらそこで確定し次の人へ。目無しなら(3回未満のうちは)もう一度振れる。3回目で目無しなら目無しで確定。役の強弱判定・最下位の振り直し・連打防止・世代トークンは維持する
- [x] T12: あみだくじ 横線後出し+爆弾表示+トゥルッ音 — 完了条件: (a)下端の支払い位置に最初から「💣」を明示表示(他はセーフが分かる表示)。上が選ぶ場所であることが分かるUI。(b)横線(rungs)は初期状態では描画せず、「結果を見る」を押したときに出現させる。(c)線をたどる際に「トゥルッ」というスライド音を鳴らす。支払者判定ロジックと世代トークンは維持する

## 後回しでいいこと

- [ ] BGM(ループ音楽)
- [ ] 傾斜配分(順位に応じた支払割合)
- [ ] 結果履歴

## 確認が必要なこと

- (現時点でなし。CEO工程でユーザー確認済み: ゲーム4種・スマホ向けWeb・全額おごり1人。効果音・チンチロ3回振り・あみだくじ爆弾表示は第2弾で追加決定)

## ファイル構成(計画)

```
projects/ogori-games/
├── README.md
├── docs/ (VISION.md / TASKS.md / QA_REPORT.md)
└── src/
    ├── index.html
    ├── css/style.css
    └── js/
        ├── app.js          # 画面遷移・参加者管理・共通結果画面
        ├── sound.js        # 効果音(WebAudio合成)共通モジュール ★第2弾で追加
        ├── roulette.js     # 男気ルーレット
        ├── high-low.js     # ハイ&ロー
        ├── chinchiro.js    # チンチロ
        └── amidakuji.js    # あみだくじ
```
